import { Fragment } from 'react';
import { Switch } from 'react-router-dom';

const PrivateRoutes = () => (
    <Fragment>
        <Switch>
        </Switch>
    </Fragment>
);

export default PrivateRoutes;
